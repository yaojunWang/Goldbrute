package rdp.gold.brute;

import rdp.gold.brute.version.Console;

public class BruteStarter {
    
    public static void main(String[] args) throws Exception {
        Console goldBruteApp = new Console();
        goldBruteApp.run(args);
    }
    
}